# 系统能力发现助手 v0.1 - 分析人员使用说明

这份说明用于产品经理、业务分析人员或 AI 分析人员，在拿到采集员导出的 ZIP 后，快速整理系统功能和抽象系统能力。

## 一、你会拿到两类 ZIP

### 1. 插件安装包

用于安装 Chrome 插件，通常由采集员使用。

### 2. 采集结果 ZIP

这是采集员完成一次系统扫描后导出的原始证据包，是后续分析的输入。

采集结果通常包含：

```text
metadata.json
summary.json
page-index.json
navigation-map.json
relationship-seeds.json
click-paths.json
pages/page-001/screenshot.png
pages/page-001/page.html
pages/page-001/page.json
pages/page-001/network.json
```

## 二、分析原则

- 不直接让 AI 生成最终能力地图。
- AI 只生成“待审核草稿”。
- 所有结论必须能回到原始证据：
  - 页面截图
  - 页面 HTML
  - 页面元数据
  - 网络请求
  - 人工备注
- 不要修改采集结果 ZIP 里的原始文件。
- 分析结果单独保存，便于重跑、回溯、纠错。
- 完全重复页面不会重复采集；疑似重复页面会保留，并在 `duplicateInfo` 中标记。
- 采集员每次点击「采集当前页」时，插件会记录点击路径，并写入 `click-paths.json`。

## 三、命名规则

提取信息和分析结果的命名，尽量贴近页面上真实出现的业务结果或关键信息。

优先级建议：

1. 人工补充的菜单路径和页面用途
2. 页面主标题、当前 Tab、面包屑
3. 表格主列名、详情字段名、筛选条件
4. 页面 URL 中可读的业务片段
5. 浏览器标题

推荐命名：

```text
Case查询
Case处理详情
Case日志
沟通及处理记录
关联单据
附件清单
```

避免命名：

```text
页面1
详情页
列表页
系统页面
接口页面
```

页面功能命名建议使用“对象 + 动作”：

```text
工单筛选查询
工单详情查看
工单处理记录查看
附件清单查看
关联单据查看
```

能力命名建议使用“业务对象 + 稳定业务动作 + 能力”：

```text
工单查询能力
工单详情查看能力
工单处理跟踪能力
工单附件管理能力
```

## 四、推荐工作流

### 第一步：按页面生成页面功能草稿

对每个 `pages/page-xxx` 单独分析，输出一个页面级 JSON。

建议输出文件：

```text
analysis/page-001.analysis.json
analysis/page-002.analysis.json
```

页面级分析重点：

- 页面名称
- 页面用途
- 所属菜单路径
- 主要对象
- 核心字段
- 核心操作
- 页面功能
- 可能的能力域
- 待人工确认点

优先读取：

- `page-index.json`：全局页面索引和候选名称
- `navigation-map.json`：菜单到页面的初始关系图
- `relationship-seeds.json`：页面内 Tab、表格、表单、API 的关系线索
- `click-paths.json`：采集员手动点击采集时形成的页面进入路径
- `pages/page-xxx/page.json`：单页详细元数据

### 第二步：人工审核页面级结果

产品经理逐页审核：

- 页面名称是否准确
- 页面用途是否符合业务
- 核心操作是否遗漏
- 菜单路径是否正确
- 能力域是否合理

如果 AI 判断不准，直接修改分析结果，不要改原始证据。

### 第三步：合并同类页面功能

把多个页面归并为功能，例如：

```text
工单查询列表
工单详情查看
工单处理记录查看
工单附件查看
工单筛选查询
```

归并时优先看：

- 菜单路径
- 页面标题
- 按钮和表格字段
- 人工备注
- 页面截图

### 第四步：抽象系统能力

把功能继续抽象成能力，例如：

```text
工单查询能力
工单详情查看能力
工单处理跟踪能力
工单附件管理能力
```

能力命名建议：

- 使用业务对象 + 动作
- 不直接使用页面标题
- 不直接使用接口名
- 避免过细或过泛

示例：

```text
页面：Case查询
功能：按条件查询工单列表、查看工单详情
能力：工单查询能力、工单详情查看能力
能力域：工单域
```

### 第五步：形成能力地图草稿

建议结构：

```json
{
  "systemName": "工单系统",
  "roleName": "CC",
  "domains": [
    {
      "domainName": "工单域",
      "capabilities": [
        {
          "capabilityName": "工单查询能力",
          "functions": [
            "按条件筛选工单",
            "查看工单列表",
            "进入工单详情"
          ],
          "evidencePages": [
            "pages/page-001",
            "pages/page-002"
          ],
          "confidence": "medium",
          "reviewStatus": "待审核"
        }
      ]
    }
  ]
}
```

## 五、从 ZIP 还原关系图

采集结果 ZIP 已经包含三个面向后续建模的索引文件：

```text
page-index.json
navigation-map.json
relationship-seeds.json
click-paths.json
```

也可以直接使用插件右侧栏的「系统还原」页面：

1. 打开插件右侧栏。
2. 切换到「系统还原」。
3. 上传采集结果 ZIP。
4. 查看系统概览、功能图、能力层次和页面索引。
5. 查看「相似页面判别」：
   - 高度重复：建议合并为一个目录节点
   - 同名但存在差异：建议拆分为子节点
   - 相似页面：需要人工确认
6. 查看「建议目录结构」，按能力域、对象、动作重新组织页面层次。
7. 点击下载按钮导出 `restored-system-model.json`。

这个还原结果是草稿，用于产品经理继续审核，不替代原始证据。

### 相似页面如何判断

系统还原页会对同名或近似命名页面做本地规则比较：

- 菜单路径是否相同
- 页面名称是否相同或近似
- 关键文本是否高度重合
- 对象线索是否一致
- URL 是否只是参数不同
- 是否已有疑似重复标记

如果不是重复页面，会显示差异点，例如：

```text
菜单路径不同
关键文本差异
对象线索差异
URL不同
```

这些差异会帮助你决定目录结构是合并还是拆分。

### 目录结构优化建议

默认建议层级：

```text
能力域
└ 业务对象
  └ 动作/场景
    └ 页面
```

例如：

```text
工单域
└ Case
  ├ 查询检索
  │ └ Case查询
  └ 详情查看
    ├ Case处理详情
    ├ Case日志
    └ 附件清单
```

### 1. 页面索引

`page-index.json` 用于快速定位页面：

```json
{
  "pageRef": "pages/page-001",
  "displayName": "Case处理详情",
  "nameCandidates": ["Case处理详情", "Case日志"],
  "menuPath": ["首页", "Case查询", "Case处理详情"],
  "keyTexts": ["Case ID", "Case状态", "操作时间"],
  "objectHints": ["Case", "工单"],
  "duplicateInfo": {
    "status": "suspected_duplicate",
    "canonicalPageId": "page-xxx",
    "confidence": "medium"
  }
}
```

用途：

- 给页面起名
- 找相似页面
- 做页面去重
- 识别疑似重复页面并人工合并
- 给 AI 分析提供上下文

### 2. 菜单关系图

`navigation-map.json` 是初始图结构：

```json
{
  "nodes": [
    { "id": "system", "name": "系统", "type": "system" },
    { "id": "menu:Case查询", "name": "Case查询", "type": "menu" },
    { "id": "page:xxx", "name": "Case处理详情", "type": "page" }
  ],
  "edges": [
    { "from": "system", "to": "menu:Case查询", "type": "contains" },
    { "from": "menu:Case查询", "to": "page:xxx", "type": "opens" }
  ]
}
```

用途：

- 还原“系统 -> 菜单 -> 页面”
- 生成 Mermaid、白板、图数据库节点
- 给能力层级提供页面来源

### 3. 关系线索

`relationship-seeds.json` 保存页面内结构线索：

```json
{
  "pageRef": "pages/page-002",
  "displayName": "Case处理详情",
  "seeds": [
    { "type": "tab", "name": "Case日志", "evidence": "visible tab text" },
    { "type": "table", "name": "表格1", "targetHint": "操作 / 操作人 / 操作时间" },
    { "type": "api", "name": "POST https://example/api/case/detail" }
  ]
}
```

用途：

- 识别页面内子功能
- 关联详情页、Tab、表格、接口
- 为“页面 -> 功能 -> 能力”抽象提供证据

### 4. 点击路径

`click-paths.json` 保存采集员点击「采集当前页」时形成的进入关系：

```json
{
  "fromPageName": "Case查询",
  "toPageName": "Case处理详情",
  "trigger": "manual_capture",
  "exactDuplicateSkipped": false
}
```

用途：

- 还原真实扫描路径
- 判断列表页如何进入详情页
- 区分菜单打开、Tab 切换、详情进入等关系
- 对完全重复页面保留“进入过”的路径记录，但不重复新增页面证据

### 5. 推荐图模型

建议把 ZIP 还原成这类层级：

```text
System
└ Menu
  └ Page
    └ Function
      └ Capability
        └ Domain
```

关系类型建议：

```text
contains：系统包含菜单，菜单包含页面
opens：菜单打开页面
clicked_to：采集员从一个页面进入另一个页面
has_function：页面承载功能
implements：功能支撑能力
belongs_to：能力归属能力域
evidenced_by：能力或功能引用原始页面证据
```

## 六、页面级 AI Prompt

```text
你是企业系统能力分析助手。

请只基于以下原始证据分析页面，不要编造不存在的信息：
1. page-index.json 中当前页面的索引项
2. navigation-map.json 中当前页面相关节点和边
3. relationship-seeds.json 中当前页面的关系线索
4. page.html
5. screenshot.png
6. page.json
7. network.json

要求：
- 输出 JSON。
- 区分事实、推测、待人工确认。
- 如果无法判断，写“无法判断”。
- 不要生成最终能力地图，只生成当前页面的功能分析草稿。
- 每个结论尽量引用 evidencePages 或 evidenceFiles。
- 命名尽量贴近页面上的业务结果或关键信息，不要使用泛称。

输出字段：
{
  "pageName": "",
  "pageNameEvidence": [],
  "menuPath": [],
  "pagePurpose": "",
  "mainObject": "",
  "userRole": "",
  "coreFields": [],
  "coreActions": [],
  "pageFunctions": [],
  "suggestedDomain": "",
  "suggestedCapabilities": [],
  "relationHints": [],
  "facts": [],
  "inferences": [],
  "questionsForReviewer": [],
  "evidenceFiles": []
}
```

## 七、能力合并 Prompt

在页面级结果经过人工审核后，再使用这个 Prompt 合并能力。

```text
你是企业系统能力建模助手。

输入是一组已经人工审核过的页面级功能分析 JSON。

请把页面功能归并为系统能力草稿。

要求：
- 不读取或修改原始证据。
- 不把单个按钮直接当成能力。
- 能力应体现稳定的业务动作。
- 每个能力必须保留来源页面 evidencePages。
- 能力命名必须贴近业务对象和操作结果。
- 对不确定的能力标记 confidence 为 low 或 medium。
- 输出仍然是待审核草稿，不是最终结论。

输出 JSON：
{
  "systemName": "",
  "roleName": "",
  "domains": [
    {
      "domainName": "",
      "capabilities": [
        {
          "capabilityName": "",
          "capabilityDescription": "",
          "functions": [],
          "relatedObjects": [],
          "evidencePages": [],
          "confidence": "low | medium | high",
          "reviewStatus": "待审核"
        }
      ]
    }
  ],
  "unresolvedQuestions": []
}
```

## 八、能力域参考

默认能力域：

```text
工单域
资产域
保修域
客户域
服务商域
组织域
知识域
项目域
结费域
任务书域
权限域
```

允许新增能力域，但必须说明依据。

## 九、建议产出物

一次采集分析完成后，建议沉淀：

```text
analysis/
├ page-001.analysis.json
├ page-002.analysis.json
├ restored-system-model.json
├ reviewed-pages.json
├ capability-map.draft.json
└ review-notes.md
```

其中：

- `page-xxx.analysis.json`：AI 页面级草稿
- `reviewed-pages.json`：人工审核后的页面功能
- `capability-map.draft.json`：能力地图草稿
- `review-notes.md`：人工纠错、待确认问题和版本记录
