# 系统能力发现助手 v0.1 - 采集员使用说明

这个插件用于在你正常浏览企业系统时，自动保存页面证据，并在结束后导出 ZIP 文件。

插件只做采集，不会生成能力地图，不会修改业务系统数据。

## 一、安装插件

1. 解压收到的插件压缩包。
2. 打开 Chrome，进入 `chrome://extensions`。
3. 打开右上角「开发者模式」。
4. 点击「加载已解压的扩展程序」。
5. 选择解压后的 `extension` 文件夹。
6. 安装成功后，浏览器右上角会出现插件图标。

## 二、开始采集

1. 打开需要采集的业务系统，并正常登录。
2. 点击浏览器右上角插件图标。
3. Chrome 右侧会打开「系统能力发现助手」侧边栏。
4. 填写采集任务：
   - 任务名称：例如 `CC角色扫描`
   - 系统名称：例如 `工单系统`
   - 角色名称：例如 `CC`
   - 采集员：填写你的姓名
5. 点击「开始采集」。

开始任务后，你在业务系统页面里点击菜单、按钮、链接、表格行等操作时，插件会在点击后自动采集当前页面。

右侧栏里的「手动补采当前页」只作为兜底使用：例如某个页面没有明显点击入口，或者你想补一张当前页面证据。

## 三、采集过程

开始采集后，你只需要像平时一样浏览系统：

- 点击菜单
- 打开列表页
- 打开详情页
- 切换标签页
- 点击查询、筛选、查看等常用操作

每次点击业务页面里的关键入口后，插件会保存：

- 页面截图
- 页面 HTML
- 页面地址和标题
- 页面按钮、输入框、表格等元素
- 页面候选名称、关键文本、主要对象线索
- 页面网络请求记录
- 点击了哪个菜单、按钮、链接或表格行
- 从上一个已采页面进入当前页面的点击路径

如果右侧面板里的「已采页面」没有增加，可以点击面板右上角的刷新按钮查看最新状态。

如果重新进入完全相同的页面，插件不会重复新增页面。  
如果页面高度相似但无法完全确认重复，插件会保留证据，并标记为「疑似重复」。

右侧栏的「已扫描结构」会实时展示当前已经扫描到的菜单、页面和结构入口。重复进入同一个页面时，插件不会重复新增页面，但会把多次相同入口聚合成一条结构关系，减少人为点击顺序对系统还原的影响。

## 四、人工补充

如果插件没有识别出正确菜单路径，可以在「页面证据」里选中页面，然后填写：

- 人工菜单路径
- 页面用途
- 用户角色
- 重要程度

填写后点击「保存备注」。

页面名称或路径尽量贴近页面上的业务信息，例如：

- `Case查询`
- `Case处理详情`
- `Case日志`
- `附件清单`
- `沟通及处理记录`

不要只写“列表页”“详情页”这类泛称。

## 五、结束并导出

1. 采集完成后，点击停止按钮结束采集。
2. 点击下载按钮导出 ZIP。
3. 把导出的 ZIP 文件交给产品经理或分析人员。

## 六、删除项目

如果某个采集项目不需要，可以在项目选择栏右侧点击删除按钮。

删除当前项目会同时删除：

- 项目信息
- 页面证据
- 网络记录
- 点击路径

如果需要清空所有历史项目，点击「清空全部项目」。这个操作只清理本地插件数据，不会影响业务系统。

## 七、查看系统还原结果

插件右侧栏里有「系统还原」页签。

如果你需要快速检查采集结果：

1. 点击「系统还原」。
2. 选择已经导出的采集结果 ZIP。
3. 插件会在本地解析 ZIP，并展示：
   - 系统概览
   - 菜单和页面关系
   - 页面索引
   - 功能候选
   - 能力草稿

这个过程只在本地浏览器中完成，不会上传文件。

## 八、注意事项

- 采集过程中尽量保持右侧边栏打开。
- 每点击进入一个重要页面后，停留 1-2 秒，让插件完成保存。
- 如果页面内容是点击后才加载出来的，等内容加载完成后再进入下一个页面。
- 不需要复制代码，不需要打开数据库，不需要修改系统配置。
- 如果插件没有反应，可以刷新业务系统页面，再点击插件右上角刷新按钮。

## 九、导出的 ZIP 包含什么

导出的 ZIP 通常包含：

```text
metadata.json
summary.json
page-index.json
navigation-map.json
relationship-seeds.json
structure-relations.json
click-paths.json
pages/page-001/screenshot.png
pages/page-001/page.html
pages/page-001/page.json
pages/page-001/network.json
```

这些文件是后续 AI 分析和产品审核的原始证据，请不要手动修改。
